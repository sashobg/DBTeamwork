@model Movies.Models.Actor
@{
    ViewBag.Title = "Изтриване на актьор";
}

<div class="container">
    <div class="well">
        <h2>Изтрий актьор</h2>
        @using (Html.BeginForm("Delete", "Actor", FormMethod.Post, new { @class = "form-horizontal" }))
        {
            <div class="form-group">
                @Html.LabelFor(m => m.FirstName, new { @class = "control-label col-sm-4" })
                <div class="col-sm-4 ">
                    @Html.TextBoxFor(m => m.FirstName, new { @class = "form-control", @readonly = "readonly" })
                </div>
            </div>

            <div class="form-group">
                @Html.LabelFor(m => m.LastName, new { @class = "control-label col-sm-4" })
                <div class="col-sm-4 ">
                    @Html.TextBoxFor(m => m.LastName, new { @class = "form-control", @readonly = "readonly" })
                </div>
            </div>

            


            <div class="form-group">
                <div class="col-sm-4 col-sm-offset-4">
                    @Html.ActionLink("Назад", "List", "Actor", new { @class = "btn btn-default" })
                    <input type="submit" value="Изтрий" class="btn btn-danger" />
                </div>
            </div>
        }
    </div>
</div>