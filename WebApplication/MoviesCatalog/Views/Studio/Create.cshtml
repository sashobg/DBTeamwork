@model Movies.Models.Studio

@{
    ViewBag.Title = "Добавяне на студио";
}

<div class="container">
    <div class="well">
        <h2>Добави студио</h2>
        @using (@Html.BeginForm("Create", "Studio", FormMethod.Post, new { @class = "form-horizontal" }))
        {
            <div class="form-group span=8 offset=2">
                @Html.LabelFor(m => m.Name, new { @class = "control-label col-sm-4" })
                <div class="col-sm-4">
                    @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
                </div>
            </div>
            
            <div class="form-group">
                <div class="col-sm-offset-4 col-sm-4">
                    @Html.ActionLink("Назад", "Index", "Studio", new { @class = "btn btn-default" })
                    <input type="submit" value="Добави" class="btn btn-success" />
                </div>
            </div>
        }
    </div>
</div>
